import { FilterbyregionPipe } from './filterbyregion.pipe';

describe('FilterbyregionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbyregionPipe();
    expect(pipe).toBeTruthy();
  });
});
